/*
const app = require("../app")("test2");
const supertest = require("supertest")(app);
const mongoose = require("mongoose")

describe("Route testing", function() {
   it("Sample test of index", function(done) {
       supertest
           .get("/")
           .expect(302)
           //.expect("Hello, World!")
           .end(done);
   })

   after(function(done) {
     mongoose.connection.db.dropDatabase()
     done()
   })
})

*/
